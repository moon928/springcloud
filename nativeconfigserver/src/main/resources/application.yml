server:
  port: 8762
spring:
  application:
    name: nativeconfigserver
#    配日志文件获取方式
  profiles:
    active: native
#    本地配日志文件存放路径
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared

