server:
  port: 8012
spring:
  application:
#    当前服务在Eureka Server的注册i名称
    name: provider

eureka:
  client:
    service-url:
#      注册中心的访问地址
      defaultZone: http://localhost:8761/eureka/

  instance:
#    是否将当前服务的ip注册当Eureka Server
    prefer-ip-address: true
